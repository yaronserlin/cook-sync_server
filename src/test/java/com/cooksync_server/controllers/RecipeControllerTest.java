package com.cooksync_server.controllers;

// Intentionally left without a test class: the previous WebMvcTest-based test here was
// entirely commented out and non-functional (see git history if it needs to be revived).
